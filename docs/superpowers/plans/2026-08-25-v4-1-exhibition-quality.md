# V4.1 展陈质量优化计划

1. 审计 `assets.json`、`sources.json` 与五个展厅映射，确保主图只使用真实任务影像；复制品和示意图分别降级为辅助影像或技术资料。
2. 中文化资产标题、说明、来源和版权表达，并将所有缺图状态统一为“影像资料待补充”。
3. 清理首页、展厅、时间线、任务档案、火箭与技术树中的英文可见文案，轨迹节点改为中文。
4. 在现有 V4 样式之上提高正文、说明、图注和交互字号，强化主图、叙事引言与辅助文字的视觉层级。
5. 运行 JSON 数据契约检查、`npm run build` 和无头浏览器运行检查，保存首页与展厅截图。

约束：不修改采集工具，不新增网络素材，不改写 React 或 Three.js 架构。
