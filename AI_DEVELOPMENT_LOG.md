# AI 辅助开发日志

## V5.0-RC1-PC

### 人工提出的目标

承接 V5.0-F，完成 V5.0 正式版本前的 PC 桌面深度终审。本轮按补充修正冻结为单一 `1600×900` 视口，只生成首页和五座真实 GrandHall 展厅共六张 `fullPage` PNG；不生成手机端、平板端、`390×844`、`768px`、`1920×1080` 或其他截图目录。移动端和平板端问题统一留到 V5.1 TODO。

### 基线检查

- `main` 与 `origin/main` 一致，基线为 V5.0-F 提交 `b7d11298eb92b2231dea985ee9f1705476fa9afe`，`v5.0-f` 已存在。
- 复用已有 Playwright、Vite 和本机 Edge/Chromium，不重复安装工具。截图脚本从 `grandHalls.json` 读取五个真实展厅 ID。
- 基线 `npm.cmd run build` 通过，但入口 JS 为约 `1,087.49 kB`，存在既有大 chunk 警告。

### RC1-PC 实际修改

- 首页 Hero 增加低对比度轨道和环形视觉重心；关键时刻区分 1970、2003、2021 三个主节点与辅助节点；共同技术底座降低连续卡片感。
- 五条主线增加键盘焦点状态，Enter/Space 可进入真实展厅，Space 不再触发页面滚动。
- 进入太空在火箭阶梯直接显示高度和近地轨道运力，并保留代表任务的参数折叠区。
- 天基中国将“我在哪里？／天气正在发生什么？／从太空能看清什么？”作为三个明确观看问题。
- 人在太空补充先做无人验证的系统逻辑、载人飞行闭环和空间站长期运营；奔向月球补足绕月捕获、动力下降、采样起飞和月轨交会动作；走向行星补充发射窗口、转移轨道、行星运动、通信时延和自主判断说明。
- `SpaceScene`、`GrandHallPage` 和 `HallPage` 使用 React lazy import；入口 chunk 降为约 `218.13 kB`，`SpaceScene` 独立 chunk 约 `816.52 kB`，总构建仍保留大 chunk 警告，未开展大规模性能重构。
- `index.html` 增加内联 favicon，消除截图验收中的 favicon 404。

### 媒体检索与来源判断

- 继续核验国家航天局、中国载人航天工程、中国气象局等官方资料；授权不够清晰的内容保留 link-only。
- 本轮没有新增本地图片或视频，没有下载大型视频，不改变既有开放许可素材登记。

### 自动验收与视觉检查

- `scripts/capture-review-pages.mjs` 改为只覆盖 `docs/screenshots/v5.0-rc1-pc/` 下的 `01-home.png` 至 `06-planetary.png`，不建立移动端、平板端、1600、1920 子目录。
- 脚本检查页面稳定、字体和图片加载、fullPage PNG 非空、错误页、Vite 错误覆盖层、同源 404、console/pageerror、broken image、图片 alt、链接命名和 1600px 横向溢出。
- 六张图已实际读取：标题无遮挡；图片比例、字号、留白、对齐、卡片高度和来源层级正常；Hero 重心可用；五厅保持差异化；没有英文普通 UI、broken image 或横向溢出。
- Playwright 交互探针确认六条真实路由无 loading 残留，主线键盘进入和火箭参数展开正常。
- `npm.cmd run build`、`npm.cmd run screenshots`、`git diff --check` 通过。移动端和平板端没有截图也没有修改。

### 当前阶段

V5.0-RC1-PC 完成 PC 单视口终审，等待用户与 ChatGPT 对六张截图进行最终验收；本轮准备创建 `v5.0-rc1-pc` 检查点，不创建正式 `v5.0` 标签。

## V5.0-F

### 人工提出的目标

在 V5.0-E4 的五座展厅、Deep Dive 第二层和来源体系基础上，继续把项目从“做得不错的专题网页”推进为“真正的数字博物馆展览”。不扩大页面数量，不推翻五条发展主线、五座主题展厅、真实 GrandHall 路由、五个独立 Story、官方媒体与开放许可素材体系，以及深色 Hero 加浅色正文的总体方向。重点转向展品主角、影像叙事、空间尺度、轻交互、来源降噪和移动端自动验收。

### 基线检查

- `main` 与 `origin/main` 一致，工作区干净，E4 最终提交为 `21d2f4a4e40f06a2d013f334e1114b2345694299`，`v5.0-e4` 已存在。
- 复用既有 Playwright 截图能力，没有重复安装依赖；脚本继续从 `grandHalls.json` 和真实路由读取五个展厅，不硬编码已失效的展厅 ID。
- E4 基线构建和桌面六页截图通过；构建保留既有 JavaScript chunk 较大警告。

### F1 实际修改

- 为 `DeepDiveSection` 增加展陈布局语义，五厅分别使用尺度、观察、章节、路线和距离布局，不再使用统一三列等宽模板。
- 进入太空采用小—中—大的重点展项节奏，长征五号成为更宽的主展品；天基中国把卫星观察网络与重点影像拆成不同层级；人在太空改为纵向三幕；奔向月球强化绕、落、回路线与月背支路；走向行星建立大主展项与次级距离节奏。
- 调整真实照片的图像、图注和来源顺序，避免长文字直接压在历史影像上；修复进入太空媒体说明的浅色背景对比度。
- 扩展自动截图脚本，固定生成桌面 `1600×900` 与移动端 `390×844` 两组 fullPage PNG。
- F1 提交：`35528ee`，`V5.0-F1：完成五厅展品化与视觉层级重构`。

### F2 实际修改

- 本厅导览增加 IntersectionObserver 驱动的当前章节状态，并保留轻量横向导览，不改成后台侧栏。
- 进入太空将火箭高度、运力和代表任务收进可键盘打开的“查看参数”折叠区，保留轮廓和尺度比较为第一视觉。
- 官方视频链接显示“观看官方影像”，其他媒体显示“查看来源”，同时补充明确的辅助标签。
- 自动截图加入内容图片替代文本、链接可识别名称、移动端横向溢出和非空截图检查；发现卫星网络在移动端超出视口后，增加局部裁切修复并重新完成双端验收。
- 增加低风险的焦点、悬停和减少动态效果处理，没有引入新的大型 WebGL、音频、PWA、账号、后台或数据库结构。
- F2 提交：`6bdf7e5`，`V5.0-F2：完善影像叙事、轻交互与来源体系`。

### 媒体检索与来源判断

- 本轮检索并核验了北斗官网关于北斗三号混合轨道星座的资料、国家航天局天问科学影像、中国气象局风云云图说明等官方资料；授权不够清晰的风云云图没有复制进本地素材，而是保持官方链接入口。
- 本轮没有新增二进制真实图片；继续使用已有的开放许可长征五号转运图、高分三号雷达影像、天问一号发射图和既有任务影像。已有本地素材按登记使用 `CC BY 4.0`、`CC BY 3.0` 等许可信息展示来源。
- 没有下载大型视频；北斗、风云、高分、载人航天、探月和天问相关官方影像继续采用 link-only 策略，避免把授权不明或体积过大的媒体写入仓库。

### F 验收结果

- F1、F2 每轮均执行修改、生产构建、桌面截图、移动端截图和视觉检查；最终没有发现需要另起 F3 的明显遮挡、英文界面、来源抢视觉或移动端布局问题。
- `npm.cmd run build` 通过；生产构建仍提示 JS chunk 大于 500 kB，本轮没有进行大规模代码拆分。
- `npm.cmd run screenshots` 通过，输出桌面六张和移动端六张，均为 fullPage PNG；脚本检查图片加载、错误页、错误覆盖层、图片替代文本、链接命名和移动端横向溢出。
- F 最终桌面与移动端截图分别归档至 `docs/screenshots/v5.0-f/` 和 `docs/screenshots/v5.0-f/mobile/`；普通开发截图继续只覆盖 `docs/screenshots/review/latest/`。

### 当前阶段

V5.0-F 完成两轮，等待人工/ChatGPT 对桌面与移动端截图做最终验收。按照本轮要求创建 `v5.0-f` 检查点，但不创建正式 `v5.0` 标签；后续只在明确需求下继续开发。

## V5.0-E4

### 人工提出的目标

在 V5.0-E3 的五大发展主线、五座主题展厅和数字博物馆视觉基底上继续做展陈深化。重点是让用户能够在每座展厅内继续向下阅读重点展项，修复人工截图中发现的 Hero 标题遮挡、视觉溢出、导览缺失和媒体卡片插入叙事流程等问题。本轮完成 E4 后停止，不进入 V5.0-F。

### E4.1 实际修改

- 新增轻量“本厅导览”，为五个独立 GrandHall Story 提供概览、核心叙事、重点展项和下一步入口。
- 为五座展厅的 Hero 文本建立标题安全区，并统一控制视觉容器的溢出；进入太空恢复六枚火箭的连续运力阶梯。
- 将长征五号转运图和长征二号 F 影像从火箭阶梯内部移到阶梯后的媒体展陈区，避免媒体卡片打断运力叙事。
- 将本轮新增开放许可素材的可见标题中文化，同时保留来源详情、作者和许可证信息的折叠机制。
- `npm.cmd run build` 与 `npm.cmd run screenshots` 通过；本轮提交为 `8df0035 V5.0-E4.1：修复展厅视觉缺陷与媒体布局`。

### E4.2 实际修改

- 新增可复用的“数字博物馆第二层”重点展项组件，每座展厅最多展示三项，统一提供展项年份、展陈说明、关键指标和来源入口。
- 五厅使用不同的视觉组合：进入太空使用历史档案、交会示意和长征五号影像；天基中国使用卫星星座、云图示意和高分三号雷达影像；人在太空使用神舟五号影像、交会示意和空间站影像；奔向月球使用三组探月阶段展项；走向行星使用天问一号、祝融号和深空示意。
- 保持五条主线与五座展厅的真实数据关系、`#/grand-hall/:grandHallId` 路由、五个独立 Story、任务级 `whyItMatters` 和展厅级 `hallWhyItMatters`；没有新增无法确认来源的媒体。
- `npm.cmd run build` 与 `npm.cmd run screenshots` 再次通过；六张截图均为 1600×900 视口的 fullPage PNG。本轮提交为 `084f6e8 V5.0-E4.2：深化五厅展陈与重点展项`。

### E4 验收结果

- E4 按要求完成两轮：E4.1、E4.2；没有因为主观堆叠而创建 E4.3。
- 自动截图脚本根据 `grandHalls.json` 读取五个真实展厅 ID，输出首页和五座展厅共六张固定命名截图；截图模式只暂停动画并提前加载图片，不改变正常网页体验。
- 最终六张截图覆盖 `docs/screenshots/review/latest/`，E4 明确版本截图归档到 `docs/screenshots/v5.0-e4/`。
- 仍需持续关注生产构建的较大 JavaScript chunk 警告，以及部分 V4 旧任务影像的授权和来源核验；这两项不在本轮 E4 的功能范围内。

### 当前阶段

E4 完成后停在 V5.0-E4，等待人工/ChatGPT 对自动验收截图进行确认，不开始 V5.0-F。E3 已建立 `v5.0-e3` GitHub 检查点；E4 使用独立的中文提交、`v5.0-e4` annotated tag 和远程推送形成新的检查点。

## V5.0-E3

### 人工提出的目标

在 V4.4.1 的任务详情基础上，继续完成 V5 的五大发展主线与五大主题展厅，并把页面从偏科技 Dashboard 的视觉方向收束为具有数字博物馆气质的展陈基底。本轮只做展陈视觉和媒体呈现整理，不进入 V5.0-F，也不进行性能专项、历史重写或大规模媒体采集。

### V5 从 V4.4.1 到 E3 的实际演进

- **数据层**：新增 `grandHalls.json` 与 `developmentLines.json`，形成进入太空、天基中国、人在太空、奔向月球、走向行星五条主线及五座主题展厅的关系数据。
- **结构层**：接入 `#/grand-hall/:grandHallId` 真实展厅路由，保留 `#/hall/:missionId` 作为既有任务详情路径；将旧任务页降为具体展项详情层。
- **叙事层**：建立五个独立 Story，分别呈现运力阶梯、卫星应用网络、载人航天阶段、月球绕落回与月背采样、行星际距离与深空任务。
- **内容层**：补齐展厅级 `hallWhyItMatters` 与任务级 `whyItMatters` 的分层表达，保留既有任务、时间线、火箭谱系、技术树和官方影像能力。
- **媒体层**：建立统一媒体卡片与 `StoryMedia` 入口，使用官方媒体链接和开放许可本地 WebP 素材；来源详情、作者、文件名、许可证链接默认折叠。
- **视觉层**：完成深浅交替的博物馆基底、低饱和五厅强调色、正文层级、卡片边框收束和必要暗场保留。
- **验收层**：建立 `scripts/capture-review-pages.mjs` 与 `npm run screenshots`，按照 `grandHalls.json` 的真实展厅 ID 自动生成六张固定命名长页面截图。

### 五大发展主线与五大主题展厅

1. 进入太空：运载火箭、发射场与测控体系。
2. 天基中国：卫星、导航、遥感与空间应用。
3. 人在太空：载人航天与中国空间站。
4. 奔向月球：中国探月工程。
5. 走向行星：火星与行星际探测。

每座展厅拥有独立的 `GrandHall Story`，并通过展厅节点进入既有任务详情页；展厅级说明回答路线为何重要，任务级说明回答具体任务为何重要。

### E3 实际修改与验证

- 首页保留首屏深色 Hero、五条主线、关键时刻和共同技术底座，减少不必要的发光、模糊、渐变与噪声。
- 五个展厅 Hero 保持深色沉浸入口，正文分别使用冷灰、浅青、浅绿、灰紫和沙灰等轻差异背景。
- 进入太空将真实长征五号转运图作为叙事媒体节点；天基中国加入高分三号雷达影像；走向行星加入天问一号发射图；人在太空与奔向月球继续复用现有真实任务影像。
- 图片、字体、动画等待和页面错误检查纳入自动截图脚本；截图模式只暂停动画并将懒加载图片改为及时加载，不改变正常网页体验。
- `npm.cmd run build` 通过；`npm.cmd run screenshots` 生成六张 1600×900 视口、fullPage PNG，并归档到 `docs/screenshots/v5.0-e3/`。

### 当前仍存在的问题

- 部分 V4 旧任务影像的授权和来源仍需持续补强，本轮没有将无法确认授权的素材批量加入。
- 构建仍提示 JavaScript chunk 较大，但性能拆分不属于 E3 范围。
- Deep Dive 第二层、轻量本厅导览、真实图片进一步放大和最终视觉终审留待 V5.0-E4。

### Git 记录说明

V5.0-A 至 V5.0-E3 的主要开发发生在 V4.4.1 之后、当前 E3 归档提交之前的工作区中。本轮阶段 A 将以单独的中文提交、`v5.0-e3` annotated tag 和远程推送建立正式检查点；没有伪造不存在的 V5 中间提交。

## V4.4.1

### 人工提出的问题

V4.4 页面需要稳定的公网访问和 main 分支自动发布，同时保持现有展厅内容不变。

### Codex 实际修改

配置 Vite /China-Space-Museum/ base，新增 GitHub Pages Actions（checkout、Node LTS、npm ci、build、上传 dist、deploy-pages），新增提示词与迭代记录，并更新 README、CHANGELOG。

### 验证结果

本地 `npm ci` 与 `npm run build` 已通过；远程 Actions、Pages 状态在发布流程中核验。


本日志记录“人工提出需求 → AI/Codex 修改 → 人工验收 → 再次迭代”的过程。所有面向人的记录使用中文；代码中的英文技术命名保持原样。

## V4.4

### 人工提出的问题

五座展厅共用的任务轨迹过于模板化，不能说明载人绕飞、月球采样返回、火星绕落巡和空间站转位建造的真实差异。

### 给 Codex 的要求

删除无解释价值的统一弧线模板，按任务性质建立不同过程布局；阶段数据集中保存在 JSON；每个过程后增加可靠官方影像外链；完成中文开发记录、构建、截图、中文提交、标签与推送。

### Codex 实际修改

建立五种任务过程组件及数据：东方红一号历史时间线、神舟五号近地绕飞、嫦娥四任务选择与地月线路、天问一号奔火/落火/巡火、空间站三舱组装。新增9条官方影像链接及外链卡片，移除展厅对旧轨迹组件的引用。

### 验证结果

任务过程与官方链接数据契约通过，npm run build 通过；本地浏览器 DOM 验证五种布局均已渲染，并生成5张滚动定位到任务过程/官方影像模块的验收截图。

### 尚待人工确认的问题

官方多媒体页面采用外链打开，没有下载大型视频，也没有使用不稳定的跨站播放器。

## V4.2

### 人工目标

将项目正式纳入 Git/GitHub 版本管理，建立中文 AI 开发记录体系，并把此前的开发阶段整理为不伪造历史提交的回顾记录。

### 给 Codex 的主要要求

创建 `.gitignore`、中文 README、CHANGELOG、AI 总日志、迭代记录目录和提示词档案目录；确认 `node_modules`、`dist`、Vite 缓存、浏览器测试数据和已确认不需要的 `src/assets/images.zip` 不进入 Git；初始化 Git，检查待跟踪文件，创建中文提交说明的 V4.2 基线提交，并创建 `v4.2` 标签。不得 push、删除文件或修改 React/Three.js 业务代码和航天数据。

完整提示词见：[V4.2 Codex 开发提示词](docs/prompts/v4.2.md)。

### Codex 完成内容

- 创建根目录 `.gitignore`，排除依赖、构建产物、缓存、临时文件、浏览器 Profile 及 `src/assets/images.zip`。
- 创建中文 `README.md`、`CHANGELOG.md` 和本日志。
- 创建 `docs/iterations/` 与 `docs/prompts/`，补充历史回顾和提示词档案说明。
- 保留业务源码、航天数据、图片素材和工具目录不变。
- 初始化 Git，检查忽略规则，建立 V4.2 基线提交和 `v4.2` 标签。

### 人工验收结果

待人工确认：首次跟踪文件数量、提交大小、提交哈希、标签和工作区状态。

### 存在问题

- `src/assets/images.zip` 仍保留在本地，但已加入忽略规则；本轮不删除。
- `docs/screenshots/` 中的截图是否全部长期保留，后续可再筛选。
- 此前 V1 至 V4.2 的历史没有对应的 Git 提交，只能作为资料回顾。

### 下一版本计划

在 V4.3 开始，每轮迭代先保存完整中文提示词，再记录 Codex 修改内容和人工验收结果。


## V4.3

### 人工提出的问题

航天图片与历史事件匹配度不足，部分馆藏模型被放在任务主视觉位置，现有技术图有外文标注，图片的展陈身份不清晰。

### 给 Codex 的要求

不重构应用架构；审计全部图片，优先补强东方红一号和神舟五号历史叙事，以中文类别标识所有图片，重绘经核对的外文技术图；完成构建、截图、中文提交、标签和推送。

### Codex 实际修改

新增两张授权和来源明确的历史/任务影像，调整东方红一号和神舟五号主视觉；保留并明确馆藏展品身份；新增 3 张中文 SVG 技术图；扩充素材来源说明；更新图片卡片文案和 V4.3 文档。

### 验证结果

JSON 数据解析和 npm run build 已通过；已生成 5 张本地页面验收截图。Git 提交与推送状态将在发布后记录。

### 尚待人工确认的问题

东方红一号和神舟五号发射现场照片的公开复用授权仍需后续持续补强；本轮没有以无法确认来源的图片填充展厅。


